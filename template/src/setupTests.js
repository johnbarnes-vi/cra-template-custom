// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';


/*
AN EXAMPLE OF AN "App.test.js" file:

    import { render, screen } from '@testing-library/react';
    import App from './App';

    test('renders learn react link', () => {
    render(<App />);
    const linkElement = screen.getByText(/learn react/i);
    expect(linkElement).toBeInTheDocument();
    });

*/

// When you run the tests using "npm test" or "yarn test", this test case will be executed along with any other test cases in your project.
// test() and expect() are dependences from jest added in setupTests.js